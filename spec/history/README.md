# spec/history — retired working canvases

Frozen proposals: each file here is the working canvas of one **closed**
development cycle (PROTOCOL_DEV, the cycle), retired verbatim at close
with a closing note — date, the seed it served, the landing records it
produced. Together they are the append-only log of this system's own
development: read, never edited. Corrections belong to the living layer —
the seed, the ADRs, the volumes.

No cycle has closed yet; the first resident will be
[design_proposal.md](../design_proposal.md) when the `guild.centre-dock`
cycle completes.
