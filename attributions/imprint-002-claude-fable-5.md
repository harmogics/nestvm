# imprint-002 — the centre-dock manifestation

**Model:** Claude Fable 5 (`claude-fable-5`), Claude Code harness.
**Human:** the architect and gate-holder — set the frames (the study
pass, the developer-language walkthroughs, the toggleability question),
challenged the read map, gave the go (2026-07-19).
**Arc:** the `guild.centre-dock` cycle from gate to
manifested-in-source: the Phase-7 gate register (design_proposal §12),
preparations P1–P5 (contract-keyed transport, the parameter space and
port, the carousel registry with its capability-filter point, the
tuples-by-offset endpoint, the widget seam with the raw-JSON floor),
then the three members — trace (paper restyle, actor tokens, the dark
drawer retired), produced-texts (block formers to the plane, the client
canvas derivation deleted), scene-detail (the kneaded product, strata
through the shared row-former registry, `composer.target` end to end).

**Materials:** the full study pass of 2026-07-19 (specifications, huid,
spec, meta, themes, CONVENTIONS first-hand); four independent parity
oracles (18 recorded sessions each) as the regression net; live smoke in
the browser at every UI step.

**How it went — honesty included.** The gate register caught real drift
before code: the «plane» terminology collision (resolved by a recorded
HUID 00 §7 amendment), two stale cross-references, stale paths. During
implementation the one seating omission — `centre.focus` absent from the
seated-modules list — was caught by live smoke, not by the compiler, and
fixed by the single line the P1 acceptance promised. The parity oracles
ran green on first pass for all three new contracts; the reviewer notes
that the fold ports were derived from an already-fold-shaped client
projection, which deserves the credit. Interim liberties (catalogue and
shelf as injected props; the host-rendered pre-scene state) are named in
the centre-focus lid with their successor (the shelf/session-meta
contract). The canvas retirement note and the cycle's closing acceptance
were left to the human, as Art. 9 requires.
